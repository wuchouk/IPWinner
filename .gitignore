# ── V1/V2 遺留 ──
.merge_history.json
.api_credentials.json

# ── 下載的 EML/附件檔案（大量、可從 Gmail/Drive 重新取得）──
下載區/
整理好的檔案/
V3-下載區-*/

# ── 測試產出的 JSON 日誌（可重新產生）──
V3-*.json

# ── macOS ──
.DS_Store
**/.DS_Store

# ── Python ──
__pycache__/
*.pyc
.venv/
